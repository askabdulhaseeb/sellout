import 'package:flutter/material.dart';

class GroupChatListWidget extends StatelessWidget {
  const GroupChatListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Group List'));
  }
}
