import 'package:flutter/material.dart';

/// ✅ Step 2: Booking
class StepBooking extends StatelessWidget {
  const StepBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Booking Screen here'));
  }
}
