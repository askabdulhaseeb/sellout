import 'package:flutter/material.dart';

class ChatInputRecordingWidget extends StatelessWidget {
  const ChatInputRecordingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}