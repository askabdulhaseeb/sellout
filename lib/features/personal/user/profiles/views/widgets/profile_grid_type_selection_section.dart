import 'package:flutter/material.dart';

class ProfileGridTypeSelectionSection extends StatelessWidget {
  const ProfileGridTypeSelectionSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 60,
      color: Colors.red,
      child: const Center(
        child: Text('Profile Grid Type Selection Section'),
      ),
    );
  }
}
