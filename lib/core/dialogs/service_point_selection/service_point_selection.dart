///
/// A reusable dialog for selecting pickup locations/service points
/// with map integration, filters, and responsive design.
///
/// Usage:
/// ```dart
/// import 'package:sellout/core/dialogs/service_point_selection/service_point_selection.dart';
///
/// final result = await ServicePointSelectionDialog.show(
///   context: context,
///   servicePoints: myServicePoints,
/// );
/// ```

library;

export 'service_point_selection_dialog.dart';
